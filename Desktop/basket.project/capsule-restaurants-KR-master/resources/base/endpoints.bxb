endpoints {  
  action-endpoints {
    action-endpoint (GetBasketInfo) {
      accepted-inputs (near, point, self, location, where,where1, addvoca)
      local-endpoint (GetBasketInfo.js)
    }
    action-endpoint (FindStore) {
      accepted-inputs (location1, place, where1,where ,near, point, self, addvoca)
      local-endpoint (FindStore.js)
    }
  }
}
